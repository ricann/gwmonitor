query.exec(QString("create table scalardata(name varchar(20),node int,time DATETIME,value double,"
                       "id int not null primary key AUTO_INCREMENT"));

query.exec(QString("insert into scalardata(name,node,time,value) values('ph',1,'2013-11-22 16:26:30',0.9)"));

while(query.next())
    {
        qDebug()<<query.value(0).toString()<<query.value(1).toInt()<<query.value(2).toDateTime()
                    <<query.value(3).toDouble()<<query.value(4).toInt();
    }
 


删除表中所有数据：
delete from 表名；
truncate table 表名；

create table scalardata(code varchar(6),name varchar(20),node int,time datetime,value double,id int not null primary key AUTO_INCREMENT)ENGINE=InnoDB DEFAULT CHARSET=gbk;

insert into scalardata(code,name,node,time,value)values('001','pH值',1,now(),5.7);

insert into scalardata(code,name,node,time,value)values('003','悬浮物',3,now(),25);


select * from scalardata where code="003" order by time desc limit 2;


mysql> create table topology(time datetime,path varchar(20),id int not null prim
ary key AUTO_INCREMENT);
